# Deny direct web access to everything in backend/ by default.
# Only backend/api/*.php should be reachable (see backend/api/.htaccess).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
